[package]
name = "udd-logging"
version = "0.1.0"
edition = "2024"
#resolver = "3"

[dependencies]
env_filter = "1.0.1"
log = "0.4.29"
paris = { version = "1.5", default-features = false, features = [
  "macros",
  "no_logger",
] } # log formatting
