# Runtime normalization overrides for raw terrain ids that collide with
# unrelated TerrainDefinition material ids in current EC data.
#
# Source of truth for these rows is the legacy terrain transcode data, but this
# file intentionally stores only the verified mixed-namespace collisions needed
# by the current ec_land runtime path.

[[mapping]]
new_id = 4
old_ids = [197, 198, 199]
