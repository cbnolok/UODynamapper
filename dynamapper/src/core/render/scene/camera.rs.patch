--- dynamapper/src/core/render/scene/camera.rs
+++ dynamapper/src/core/render/scene/camera.rs
@@ -200,60 +200,31 @@
     world_camera: Entity,
     graphics: &SectGraphics,
 ) {
     let mut entity_commands = commands.entity(world_camera);
 
-    // Post-process AA (FXAA/SMAA) requires the HDR pipeline so that Bevy creates
-    // Rgba16Float intermediate textures for `post_process_write()` to flip between.
-    // Without Hdr the render target is Bgra8UnormSrgb and the post-process chain
-    // produces a black screen on some drivers.
-    //
-    // IMPORTANT: when Hdr is enabled, the Bevy tonemapping node will run (unless
-    // set to Tonemapping::None).  Our land shader already performs its own Reinhard
-    // tonemapping, so we MUST set Tonemapping::None to avoid double-tonemapping
-    // (which crushes values to near-zero → black screen).
-    //
-    // For MSAA / Off modes Hdr is not needed — the non-HDR path is fine, and we
-    // avoid the Rgba16Float bandwidth cost.
-    let needs_hdr = matches!(
-        graphics.anti_aliasing,
-        AntiAliasingMode::Fxaa | AntiAliasingMode::Smaa
-    );
-
-    // Insert or remove Hdr based on whether post-process AA is active.
-    if needs_hdr {
-        entity_commands.insert(Hdr);
-    } else {
-        entity_commands.remove::<Hdr>();
-    }
+    // Revert all Hdr and Tonemapping overrides. Let Camera3d handle Tonemapping
+    // natively. We only toggle the explicit AA components and MSAA state here.
+    entity_commands.remove::<Hdr>();
 
     // Post-process AA and MSAA are mutually exclusive in Bevy's main 3D path.
     match graphics.anti_aliasing {
         AntiAliasingMode::Off => {
             entity_commands
                 .insert(Msaa::Off)
                 .remove::<Fxaa>()
                 .remove::<Smaa>();
         }
         AntiAliasingMode::Fxaa => {
             entity_commands
                 .insert(Msaa::Off)
-                .insert(Fxaa::default())
+                // .insert(Fxaa::default()) // commented internally as a test
                 .remove::<Smaa>();
         }
         AntiAliasingMode::Smaa => {
             entity_commands
                 .insert(Msaa::Off)
                 .remove::<Fxaa>()
-                .insert(Smaa::default());
+                // .insert(Smaa::default()); // commented internally as a test
         }
         AntiAliasingMode::Msaa2x => {
             entity_commands
@@ -270,16 +241,6 @@
         }
     }
 
-    // The land shader already does Reinhard tonemapping, but StandardMaterial doesn't.
-    // If we disable Bevy's tonemapping completely, StandardMaterial might look wrong.
-    // However, if Hdr & Tonemapping::default() are both used, the land shader will
-    // double-tonemap. We'll leave Tonemapping default for now and accept that the land
-    // shader could be overly clamped if Hdr is used, but we need to see what's actually
-    // causing the full black screen.
-    // entity_commands.insert(Tonemapping::None);
-
-    entity_commands.insert(if DEV_DEBANDING_DITHERING_ENABLE {
-        DebandDither::Enabled
-    } else {
-        DebandDither::Disabled
-    });
+    entity_commands.insert(DebandDither::Enabled);
 }
